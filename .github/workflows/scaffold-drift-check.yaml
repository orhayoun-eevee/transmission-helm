name: Scaffold Drift Check

on:
  pull_request:
    paths:
      - 'Makefile'
      - 'scripts/bump-version.sh'
      - '.github/workflows/on-pr.yaml'
      - '.github/workflows/on-tag.yaml'
      - '.github/workflows/renovate-config.yaml'
      - '.github/workflows/renovate-snapshot-update.yaml'
      - '.github/workflows/scaffold-drift-check.yaml'
  push:
    branches:
      - main
    paths:
      - 'Makefile'
      - 'scripts/bump-version.sh'
      - '.github/workflows/on-pr.yaml'
      - '.github/workflows/on-tag.yaml'
      - '.github/workflows/renovate-config.yaml'
      - '.github/workflows/renovate-snapshot-update.yaml'
      - '.github/workflows/scaffold-drift-check.yaml'

permissions:
  contents: read

jobs:
  drift-check:
    uses: orhayoun-eevee/build-workflow/.github/workflows/scaffold-drift-check.yaml@a3c215681f3fa7359255efca3815af8a9bf0ccda
    with:
      build_workflow_ref: a3c215681f3fa7359255efca3815af8a9bf0ccda
      repo_name: transmission-helm
    secrets:
      gh_app_id: ${{ secrets.GHCR_AUTO_APP_ID }}
      gh_app_private_key: ${{ secrets.GHCR_AUTO_PKEY }}
